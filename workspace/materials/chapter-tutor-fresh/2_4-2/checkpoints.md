# Checkpoints

- What area does convolution add?
- Which variable moves through the integral at a fixed output time?
- The fixed support starts at −1; the moving edge ends at t+2. When can overlap begin?
- At t=2 in Example 2.10, which interval contributes?
- At t=0 in Example 2.11, what is c(0)?
- At t=1.5 in Example 2.12, what are the overlap and area?
- For supports [a,b] and [c,d], where can the convolution be nonzero?