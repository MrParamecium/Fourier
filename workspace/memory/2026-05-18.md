## 2026-05-18 Homepage ask module position and balance icon
- Harrison asked to make the small graduation-cap icon above the homepage ask title slightly larger, move the whole home ask interface downward, and fix the Balanced scales icon based on the provided EduTech/Phosphor reference code.
- Increased the homepage `.home-ask-icon` from 26px to 34px and pushed `.home-ask-stage` downward with a larger top padding (`clamp(132px, 20vh, 190px)`).
- Replaced the incorrect triangle-like Balanced icon SVG with a hand-drawn scales icon using a center post, crossbar, two hanging balance pans, and base line.
- Updated both static Learn Q&A markup in `app/index.html` and runtime `learnModeIconSvg.balanced` in `app/app.js`, so the dropdown and selected mode button stay consistent after mode changes.
- Updated cache versions in `app/index.html` to `1778674216`.
- Verification: `npm run check` passed and `git diff --check -- app/index.html app/style.css app/app.js` passed. Playwright verification confirmed `style.css?v=1778674216`, homepage cap icon 34x34, title top about 232px, composer top about 380px, and the Balanced SVG path uses the new scales drawing.

## 2026-05-18 Homepage further downward shift and scales redraw
- Harrison asked to move the homepage ask module further down, then clarified that the Balanced icon still was not right.
- Increased homepage ask stage top padding again to `clamp(160px, 23vh, 220px)` so the centered ask module sits lower on the dashboard.
- Replaced the Balanced scales drawing again with a closer Phosphor-style icon: vertical post, top crossbar, two U-shaped hanging pans, and base line.
- Updated the icon in both `app/index.html` static Learn Q&A markup and `app/app.js` runtime mode icon map.
- Updated cache versions in `app/index.html` to `1778674218`.
- Verification: `npm run check` passed and `git diff --check -- app/index.html app/style.css app/app.js` passed. Playwright verification showed `style.css?v=1778674218`, icon top about 207px, title top about 259px, composer top about 407px, and the selected Balanced icon uses the new U-pan scales path.

## 2026-05-18 Slanted Balanced scales icon and tiny downward nudge
- Harrison pointed out that the reference scales icon has a left-lower/right-upper slanted beam, and asked to continue fixing it plus move the homepage down just a tiny bit.
- Replaced the Balanced icon again with a slanted-beam scales SVG: beam runs from lower-left to upper-right, with two suspended U-shaped pans and a base, closer to the Phosphor `ph-scales` reference.
- Nudged the homepage ask stage top padding from `clamp(160px, 23vh, 220px)` to `clamp(172px, 24vh, 232px)`.
- Updated both `app/index.html` static Learn Q&A mode icons and `app/app.js` runtime `learnModeIconSvg.balanced`.
- Updated cache versions in `app/index.html` to `1778674219`.
- Verification: `npm run check` passed and `git diff --check -- app/index.html app/style.css app/app.js` passed. Playwright verification showed `style.css?v=1778674219`, title top about 268px, composer top about 416px, and the Balanced path uses the slanted beam.

## 2026-05-18 Desktop tutor agent takeover in new thread
- Harrison asked in Chinese whether Codex could see the Desktop `tutor agent` folder, read memory, and take over.
- Confirmed the current generated Codex workspace `/Users/chenghaoxiang/Documents/Codex/2026-05-18/tutor-agent` is empty, and the active project is `/Users/chenghaoxiang/Desktop/tutor agent`.
- Read project handoff/context: `README.md`, `PROJECT_STRUCTURE.md`, `workspace/memory/MEMORY.md`, recent memories `workspace/memory/2026-05-18.md`, `2026-05-17.md`, `2026-05-16.md`, plus global `/Users/chenghaoxiang/Desktop/Memory/MEMORY.md`.
- Current understanding: runtime app is root `app/`, bridge is `app/ws-bridge.js`, runtime materials are root `materials/`, broader memory/workbench is `workspace/`, and localhost is served on port 9000.
- Verified `npm run check` passes and `http://127.0.0.1:9000/health` returns ok with old=104 and new=329 indexed pages.
- Current worktree is ahead of origin by 2 commits and has active uncommitted changes in `app/app.js`, `app/index.html`, `app/style.css`, `workspace/memory/2026-05-17.md`, and today's memory file; preserve these as ongoing project work and do not revert casually.

## 2026-05-18 Learn Q&A rectangle frame removal
- Harrison pointed out there was still a pale rectangular frame behind the Learn right-side Ask your Tutor empty state and asked for it to be removed.
- Added a final CSS override block `LEARN_QA_RECTANGLE_FRAME_REMOVAL` in `app/style.css` to neutralize backgrounds, borders, shadows, and pseudo-elements on `#learnChatCol`, `.learn-chat-scroll`, `#learnChatEmptyState`, `.learn-chat-empty-state`, `.edu-tutor-empty`, and `.edu-tutor-empty-inner`.
- Kept a soft gradient/background on `#learnChatCol` itself so the panel still reads as a full-page stage instead of a framed card.
- Updated the cache-busted asset version in `app/index.html` to `1778674220`.
- Verification: `npm run check` passed, `git diff --check -- index.html style.css app.js` passed, and the health endpoint still reports ok; a previous Playwright page load timed out, so the visual confirmation is not fully closed yet.

## 2026-05-18 Learn Q&A collapsed column fix
- Harrison reported the Learn right-side panel still showed a pale vertical frame even after the empty-state cleanup.
- Traced the remaining artifact to the collapsed `#learnChatCol` itself: the final runtime style injection had forced the chat column back to `display:flex`, overriding the original collapse behavior.
- Added an explicit `#learnView #learnBody.chat-collapsed #learnChatCol { display:none !important; ... }` override inside the final runtime style injection so the collapsed right column no longer paints any background, border, or width.
- Bumped the cache-busted assets in `app/index.html` to `1778674222`.
- Verification: `npm run check` and `git diff --check -- index.html style.css app.js` passed, and Playwright confirmed `#learnChatCol` now computes to `display: none` in the collapsed Learn state.

## 2026-05-18 Learn mode dropdown Phosphor glassmorphism rebuild
- Harrison asked to refactor the right Learn Q&A bottom Fast/Balanced/Detailed model-style dropdown to match a top-tier iOS/macOS floating glass component and replace the old misaligned icon structure with Phosphor icons.
- Updated `app/index.html`: reintroduced the Phosphor icon web script, replaced the full `#learnModeMenu` markup with the requested three-button structure using `ph-lightning`, `ph-scales`, and `ph-magnifying-glass`, added `.edu-mode-text` wrappers, and updated the current mode button to use the same Phosphor icon structure.
- Updated cache versions in `app/index.html` to `1778674224` for both `style.css` and `app.js`.
- Updated `app/app.js` mode sync so the visible current mode chip swaps Phosphor icon classes instead of injecting inline SVG, while preserving hidden select, `data-learn-mode`, existing click handlers, and external request logic.
- Added final glassmorphism dropdown CSS in `app/style.css`, and mirrored the same menu rules inside the runtime injected Learn Q&A style block in `app/app.js` so old overrides cannot win in the browser.
- Verification: `npm run check` passed, `git diff --check -- app/index.html app/app.js app/style.css` passed, `/health` returned ok, browser DOM checks confirmed Phosphor script/icon structure and cache versions, and Playwright DOM-click verification confirmed menu open state, 250px/20px glass styling, Balanced default highlight, Detailed click sync, hidden select sync, current button icon sync, and no console errors.

## 2026-05-18 Removed Learn Q&A top window controls
- Harrison showed the two square/minus buttons at the top of the right Learn Q&A panel and asked to delete them.
- Removed the `learn-chat-topbar` control block from `app/index.html`, including `#learnChatShrinkBtn` and `#learnChatMinimizeBtn`.
- Cleaned related dead JS constants/listeners in `app/app.js` and removed unused `.learn-chat-window-actions` / `.learn-chat-window-btn` CSS from `app/style.css`.
- Kept the popover controls (`#learnChatPopoverDockBtn`, `#learnChatPopoverCloseBtn`) intact because those belong to the separate movable Q&A window state.
- Verification: `npm run check` passed, `git diff --check -- app/index.html app/app.js app/style.css` passed after trimming one trailing space, and a static DOM/code check confirmed the two top-panel controls are gone while popover controls remain.

## 2026-05-18 Hard removal for stale Learn Q&A top controls
- Harrison showed a live browser screenshot where the square/minus buttons still appeared at the top-right of the Learn Q&A panel after the initial removal.
- Confirmed server HTML no longer contains `#learnChatShrinkBtn` / `#learnChatMinimizeBtn`, so the visible issue was likely stale DOM/cache or a residual old-style path in the active browser tab.
- Added a stronger fix: bumped `style.css` and `app.js` cache versions to `1778674226`, added CSS hard-hide rules for `#learnChatShrinkBtn`, `#learnChatMinimizeBtn`, `.learn-chat-window-actions`, and `.learn-chat-window-btn`, and added startup JS removal for those stale nodes.
- Verification: `npm run check` passed, `git diff --check -- app/index.html app/app.js app/style.css` passed, server HTML shows `1778674226` and no top window buttons, and browser DOM verification at `http://127.0.0.1:9000/?v=1778674226` confirmed `shrink=false`, `minimize=false`, `windowActions=0`, `windowBtns=0`, with popover controls still present.

## 2026-05-18 Learn Q&A empty-state overlap bug fix
- Harrison clarified that the actual Learn Q&A bug was not the square/minus buttons: when a question/loading answer appears in the right panel, the Q&A content was rendering above the `Ask your Tutor` empty hero instead of hiding the empty hero and centering the active thread.
- Root cause: `updateLearnChatEmptyState()` only checked `learnChatContent.textContent.trim()`, which was not robust for loading/progress DOM states, and later CSS overrides could keep the empty state visually present.
- Updated `app/app.js` so active chat detection uses `learnChatContent.children.length || textContent`, toggles `is-chat-active` on `#learnChatContent`, `#learnChatScroll`, and `#learnChatCol`, and applies inline `display:none !important` / hidden / opacity 0 to `#learnChatEmptyState` while chat is active, removing those inline properties when empty.
- Added final CSS in `app/style.css` so active Q&A hides the empty hero and centers the active thread/progress bubble in the right panel with max-width 560px.
- Bumped `style.css` and `app.js` cache versions to `1778674227`.
- Verification: `npm run check` passed, `git diff --check -- app/index.html app/app.js app/style.css` passed, and Playwright simulation confirmed active chat produces `emptyDisplay: none`, `learnChatContent.is-chat-active`, centered content (`margin:auto`, `align-self:center`), and full-width bubble inside the centered thread.
