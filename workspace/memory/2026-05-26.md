# 2026-05-26

## Desktop tutor agent takeover
- Harrison asked in Chinese: "你看到我桌面上的tutor agent文件夹了吗，你读取记忆并接管".
- The generated Codex projectless cwd `/Users/chenghaoxiang/Documents/Codex/2026-05-26/tutor-agent` is empty/non-canonical.
- Located the canonical active project at `/Users/chenghaoxiang/Desktop/tutor agent`.
- Read takeover context from `workspace/memory/MEMORY.md`, recent daily memories `2026-05-23.md` through `2026-05-25.md`, root `README.md`, `PROJECT_STRUCTURE.md`, `docs/WINDOWS_RAG_HANDOFF.md`, and global `/Users/chenghaoxiang/Desktop/Memory/CODEX_ONBOARDING.md`.
- Current understanding: runtime UI/API live in root `app/`; server entry is `app/ws-bridge.js`; runtime materials may resolve to `workspace/materials` first for the running server; `workspace/` stores memory/workbench/mirrors. Recent active work includes RAGFlow over userspace Tailscale proxy, profile-free lesson generation cache behavior, lesson-generation RAGFlow retrieval, logo PNG replacement/scaling, Learn/Textbook/Overview layout fixes, and multiple frosted-glass UI redesigns.
- Current repo state: `main` is ahead of `origin/main` by 5 commits. Existing modified files include `app/app.js`, `app/index.html`, `app/style.css`, `app/ws-bridge.js`, `workspace/materials/lesson-cache/1_1-1/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`, and `workspace/memory/2026-05-21.md`; untracked include app logo PNG/JPG files, lesson-cache folders `2_1`, `2_3`, `2_8`, and memory files `2026-05-22.md` through `2026-05-26.md`. Preserve existing changes and do not revert casually.
- Verification baseline: `npm run check` passed. `http://127.0.0.1:9000/health` returned ok with indexed pages `old=104`, `new=329`, `apiAsk=true`, and `apiTutor=true`.

## Windows remote RAG condition clarification
- Harrison asked whether simply turning on the Windows computer and connecting it to Wi-Fi means RAG works from anywhere.
- Clarified: distance does not matter if both machines are online in the same Tailscale tailnet, but Windows Wi-Fi alone is not enough. Windows must be awake, connected to internet/Wi-Fi, logged into Tailscale, RAGFlow must be running and listening, and the Mac/Fourier side must have Tailscale routing or the existing userspace Tailscale proxy running (`RAGFLOW_PROXY=http://127.0.0.1:1056`).
- Current known working path from recent tests: Mac app reaches Windows RAGFlow at `100.106.128.64:9380` through the local userspace Tailscale HTTP proxy and lesson generation successfully retrieved 5 RAGFlow chunks.

## Home Ask composer/menu geometry repair
- Harrison showed the Home Ask mode selector menu still opening downward over the tool chips, the composer not visibly popping on click, and the composer/menu blocking the Formula/Screenshot/Exam Trap buttons. A later screenshot showed an intermediate fix made the composer too narrow, changed perceived font/scale, and placed the whole module too high.
- Used the `frontend-design` workflow and fixed the final Home Ask CSS locks in `app/style.css`.
- Root cause: older EOF rules pushed `.home-feature-actions` down when the menu opened, and the `.home-ask-stage` CSS grid auto column collapsed to the width of the tag row, which capped the composer around 774px even when later rules requested 1080px.
- Added a true final Home Ask restore/fix layer: `.home-ask-stage` now has `grid-template-columns:minmax(0,1fr)`, the module is shifted lower, the composer is restored to 1080px desktop width, heading and tag sizes/fonts are restored, feature chips regain larger proportions, the mode menu opens upward into the reserved gap, and the composer focus pop uses `home-ask-size-restore-pop`.
- Bumped `app/index.html` stylesheet cache to `style.css?v=1778674361`.
- Verification passed: `npm run check`, `git diff --check app/style.css app/index.html`, and Playwright geometry QA. QA measured composer width 1080px, h1 48px, tags 22px/46px high, menu above the toggle, and no overlap with tags, composer, feature buttons, or h1; focus animation is `home-ask-size-restore-pop` for 0.36s.

## Home Ask rebuilt to match Learn Q&A panel
- Harrison clarified with screenshots that Home Ask should follow the Learn lesson Q&A panel layout, not the previous centered/floating Home layout.
- Added a later final CSS layer `TRUE EOF HOME ASK LEARN-QA MATCH` in `app/style.css`: Home Ask now mirrors the Learn Q&A rhythm with title/chips at the top, large vertical breathing room, a wide bottom glass composer (`1500px` desktop width, `224px` height), larger input text/buttons, bottom action buttons below the composer, and the mode menu opening upward into the empty gap.
- Bumped `app/index.html` stylesheet cache through `style.css?v=1778674364`.
- Verification passed: `npm run check`, `git diff --check app/style.css app/index.html`, and Playwright geometry QA. Final QA measured composer `1500x224`, menu above the toggle with `8px` gap above the composer, no overlap with tags/composer/feature buttons, and focus animation `home-ask-learn-qa-pop` for `0.36s`.

## Home Ask scale correction
- Harrison said the Learn-QA-matched Home Ask became much too large. Screenshot showed oversized composer, huge feature buttons, and the four feature buttons wrapping into two rows.
- Added final `TRUE EOF HOME ASK SCALE CORRECTION` CSS layer in `app/style.css`.
- Reduced Home Ask to panel proportions: composer `1040px` desktop width and `148px` height, feature buttons `44px` high in one row, h1 around `44px`, tags around `19px`, input text around `20px`, mode toggle `150px`, and mode menu moved farther upward so it no longer overlaps the composer.
- Bumped stylesheet cache to `style.css?v=1778674366`.
- Verification passed: `npm run check`, `git diff --check app/style.css app/index.html`, and Playwright QA. Final QA measured composer `1040x148`, feature chips all on one row at top `1050`, menu has `10px` gap above composer, and no overlap with composer/tags/features.

## Desktop tutor agent takeover in tutor-agent-2 thread, second 2026-05-26 pass
- Harrison asked in Chinese: "你看到我桌面上的tutor agent文件夹了吗，你读取记忆并接管".
- Located and confirmed the canonical active project at `/Users/chenghaoxiang/Desktop/tutor agent`; the current generated Codex cwd `/Users/chenghaoxiang/Documents/Codex/2026-05-26/tutor-agent-2` is only the projectless chat workspace.
- Read project takeover context from `workspace/memory/MEMORY.md`, today's `workspace/memory/2026-05-26.md`, recent memories `2026-05-23.md` through `2026-05-25.md`, `README.md`, `PROJECT_STRUCTURE.md`, `docs/WINDOWS_RAG_HANDOFF.md`, and global `/Users/chenghaoxiang/Desktop/Memory/CODEX_ONBOARDING.md` plus `/Users/chenghaoxiang/Desktop/Memory/MEMORY.md`.
- Current understanding: runtime UI/API live in root `app/`; server entry is `app/ws-bridge.js`; runtime lesson cache currently resolves through `workspace/materials` for the running server; `workspace/` stores memory, workbench material, and mirrors. Recent active work includes Home Ask geometry repair, frosted-glass UI polish, Learn/Textbook/Overview fixes, Mistake Notebook/Feedback/Course Tracker redesigns, profile-free lesson cache behavior, and RAGFlow lesson retrieval through a userspace Tailscale proxy.
- Current repo state remains `main...origin/main [ahead 5]` with existing modified files `app/app.js`, `app/index.html`, `app/style.css`, `app/ws-bridge.js`, `workspace/materials/lesson-cache/1_1-1/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`, and `workspace/memory/2026-05-21.md`; untracked include app logo files, lesson-cache folders `2_1`, `2_3`, `2_8`, and memory files `2026-05-22.md` through `2026-05-26.md`. Preserve all existing changes and do not revert casually.
- Verification baseline in this pass: `npm run check` passed, and `http://127.0.0.1:9000/health` returned ok with indexed pages `old=104`, `new=329`, `apiAsk=true`, and `apiTutor=true`.

## Lesson generation discussion kickoff
- Harrison said: "我们现在来研究探讨生成讲解的部分吧".
- Read the current lesson generation chain in `app/ws-bridge.js` and frontend lesson rendering in `app/app.js` without changing product code.
- Current generation architecture: `/api/section` and `/api/pregen/section` route into `preGenerateSectionLesson`; mapped textbook pages are collected, optional lesson-only RAGFlow chunks are appended or used as replacement context depending on `LESSON_RAGFLOW_MODE`, web sources are collected, then `generateSectionLesson` runs Agent A planner followed by Agent B tutor writer.
- Agent A uses `workspace/materials/prompts/agent-a-planner.md` plus in-code lesson rules to produce a strict JSON blueprint; Agent B uses `workspace/materials/prompts/agent-b-tutor.md` plus the same policy appendices to produce rendered JSON blocks, which are converted to markdown, normalized, validated, cached, and later rendered by the frontend as knowledge-point pages.
- Important current design: standard lessons are now profile-free base cache (`baseLessonMemory = null`), while personalized Q&A remains on `/api/ask`; cache version is `aquarius_visual_latex_v2`; generated lessons are required to use the normal Agent A -> Agent B chain and must not silently fall back to hand-written markdown.
- Main research framing to discuss with Harrison: the system already has many prompt-level teaching rules, but needs a stronger quality-evaluation and observability loop for generated lessons, especially checking concept coverage, formula locality, visual usefulness, exam orientation, examples/traps, and whether the generated quiz matches the lesson body.

## Lesson generation flowchart request
- Harrison asked: "你给我把生成讲解流程图画出来".
- Need respond with a current-code flowchart of the lesson generation pipeline: frontend/API request, cache lookup, textbook page mapping, optional parent prelude, optional RAGFlow lesson chunks, web source collection, Agent A blueprint planning, Agent B execution, markdown conversion, normalization/format validation, cache write, API response, and frontend rendering into knowledge-point pages.

## Correction: user click does not pre-generate lesson
- Harrison corrected the previous flowchart and asked why it showed a "用户点击某个 section / 预生成按钮" and a force pregen path, since the product rule should be: user clicking a section only reads cache; if no cache, do not display/generate, and generation should happen only when Harrison explicitly asks Codex to generate.
- Rechecked current code. For normal lesson clicks, `app/app.js` `openLearnMode()` auto-calls `startLesson({ silent: true })`, and `startLesson()` posts `/api/section` with `mode:'lesson'`. In `app/ws-bridge.js`, `/api/section` `mode === 'lesson'` checks `readLessonCache`; on miss it logs "runtime generation disabled" and returns `cacheMiss:true` plus `This section has not been prepared yet.` It does not call `preGenerateSectionLesson`.
- `/api/pregen/section` still exists as a manual/maintenance endpoint, and CLI `--pregen-section` also exists. There is no normal visible "pre-generate button" in the user click path based on the checked frontend references.
- Potential real logic issue: `/api/section` `mode === 'overview'` can auto-call `preGenerateSectionLesson(... cacheVariant:'parent_prelude' ...)` if a parent prelude exists and no valid cached prelude is present. This is separate from normal lesson generation, but it may violate Harrison's desired "only generate when I explicitly ask" rule if parent overview prelude generation should also be manual-only.

## Redrawn lesson-generation flowchart request
- Harrison asked: "你再重新读代码给我画流程图".
- Re-read relevant code in `app/app.js` around `openLearnMode()` and `startLesson()`, `/api/section` and `/api/pregen/section` routing in `app/ws-bridge.js`, and `preGenerateSectionLesson()`.
- Correct flowchart should separate three paths: normal user lesson click (`/api/section mode='lesson'`, cache-only, runtime generation disabled), overview parent-prelude path (`/api/section mode='overview'`, may auto-generate parent_prelude currently), and manual generation path (`/api/pregen/section` or CLI `--pregen-section`, then Agent A/B and cache write).

## RAGFlow vs local OCR lesson-source clarification
- Harrison asked whether the target is for RAG to fetch all lesson-generation context directly from the Windows machine rather than from Mac local OCR.
- Clarified current implementation: manual lesson generation still uses local mapped OCR pages as the primary `localPages`, then optionally appends Windows RAGFlow chunks via `collectLessonRagFlowPages`; with `LESSON_RAGFLOW_MODE=replace`, it still keeps `localPages.slice(0,1)` plus RAGFlow chunks. So the current code is not yet "all content from Windows RAGFlow".
- Product/architecture target if Harrison wants true RAG: Windows RAGFlow should be the semantic textbook retrieval source for lesson content; Mac local OCR/page maps can remain only as UI/navigation fallback and textbook image/page metadata, while Agent A/B should mainly consume Windows-returned chunks/citations. RAGFlow itself may be built from OCR/PDF text, but the app should not do local keyword/OCR retrieval as the lesson-generation content source.

## Lesson generation RAG architecture recommendation
- Harrison asked whether lesson-generation RAG really needs to move fully to Windows RAGFlow, since the generation task already knows the exact textbook section and the Mac OCR has all text for that section.
- Recommended architecture judgment: for standard lesson generation, local deterministic section OCR/page mapping should remain the primary source of truth because the task is "known section -> use all text", not "unknown query -> retrieve relevant chunks". Pure vector/semantic RAG can miss chunks/formulas from the exact section due to top-k limits and chunk scoring.
- Recommended split: keep lesson generation as deterministic local OCR + figure/page metadata, optionally append Windows RAGFlow as supplemental context for neighbor sections, definitions, cross-references, or OCR recovery; use Windows RAGFlow as primary for personalized Q&A and broad semantic search. Add stronger generation QA/coverage checks rather than replacing the source pipeline immediately.

## Opinion on deleting lesson-generation RAG
- Harrison said if OCR already has all textbook section text, maybe the lesson-generation RAG logic should simply be deleted, and asked for an honest judgment rather than agreement.
- Judgment: direction is mostly correct for standard lesson generation. Since generation is section-known and coverage-oriented, deterministic OCR is better than semantic top-k retrieval. Lesson RAGFlow adds dependency, latency, and possible formula/chunk omissions without much upside.
- Recommended nuance: remove or hard-disable only the lesson-generation RAGFlow hook, not the RAGFlow retrieval used by `/api/ask`. The current code risk is that `LESSON_RAGFLOW_ENABLED` inherits `RAGFLOW_ENABLED`, so enabling Q&A RAGFlow can unintentionally enable lesson-generation RAGFlow too. Safer architecture is: lesson generation = local OCR only by default; Q&A = RAGFlow semantic retrieval.

## Removed lesson-generation RAGFlow hook
- Harrison instructed: "那么你把我的讲解的rag逻辑彻底删除吧，就只用我的ocr".
- Edited `app/ws-bridge.js` only.
- Removed lesson-specific env/config constants `LESSON_RAGFLOW_ENABLED`, `LESSON_RAGFLOW_TOP_K`, and `LESSON_RAGFLOW_MODE`.
- Removed lesson-only helper functions `buildLessonRagFlowQuery`, `ragFlowChunksToLessonPages`, and `collectLessonRagFlowPages`.
- Updated `preGenerateSectionLesson()` so `rawPages = localPages`; generated lessons now use deterministic local OCR/prelude pages only and no longer append or replace with Windows RAGFlow chunks.
- Updated `inspectLessonGenerationContext()` to be synchronous OCR-only and return `lessonContextSource: 'local_ocr'`, `mappedPageCount`, `localPageCount`, `finalBookPageCount`, and `localPages`; it no longer reports lesson RAGFlow chunk counts or pages.
- Preserved general RAGFlow retrieval helpers and `/api/ask` RAGFlow behavior; remaining `ragFlowChunkCount` reference is in the Q&A debug response, not lesson generation.
- Verification: `rg` confirmed no remaining lesson-specific RAGFlow identifiers; `npm run check` passed; `git diff --check app/ws-bridge.js` passed.

## OCR-only lesson generation flowchart request
- Harrison asked for a simplified flowchart of the current lesson generation logic after deleting lesson-generation RAG.
- Need answer with concise Mermaid diagram showing manual trigger -> `/api/pregen/section`/CLI -> `preGenerateSectionLesson` -> section page map -> local OCR/prelude pages -> optional web sources -> Agent A blueprint -> Agent B rendered blocks -> markdown conversion -> normalization/validation -> write lesson cache. Mention user clicks only read cache and are separate from generation.

## Section OCR completeness check
- Harrison asked whether the OCR used for lesson generation is perfectly aligned to each small section with no extra or missing content, and asked to open it for inspection and explain how to know.
- Checked code path: for new-book lessons, `preGenerateSectionLesson()` uses `getPagesForSection()` from `app/section-page-map-new.json`, then `attachSectionOcrToPages()` reads `workspace/materials/new-book-section-ocr/<section>.txt` and attaches the entire section OCR as `ocrOverrideText` to the first mapped page while blanking OCR on later pages. Agent A then consumes that `ocrOverrideText`, so generation uses the per-section OCR file when present.
- Opened `workspace/materials/new-book-section-ocr/1.1-1.txt` and `1.1-1.meta.json`: it starts at `1.1-1 Signal Energy`, ends before `1.1-2 Signal Power`, with `foundStart=true`, `foundEnd=true`, `sourcePages=["page-065"]`, `missingPages=[]`, and `charCount=1172`. This sample is well aligned.
- Read `app/scripts/build-new-book-section-ocr.js`: section OCR is built from `section-page-map-display-new.json`, concatenates mapped page OCR, finds the section heading as start, then finds the next section/subsection heading as the end boundary. This is deterministic but depends on OCR detecting headings cleanly.
- Audited all `new-book-section-ocr/*.meta.json`: 147 section OCR files exist; 143 found starts; 122 found end boundaries. 30 files are flagged by the report for missing start/end, missing pages, or very short content.
- Concrete bad/risky example: `1.1-2.meta.json` has `foundStart=true` but `foundEnd=false`, source pages `page-065` through `page-070`, and charCount 15922. Its OCR includes later examples/drills through page 070 because no end boundary was found. So it may contain extra content, not a perfectly bounded subsection.
- Conclusion to tell Harrison: cannot honestly guarantee all section OCR is exactly no-more/no-less. Many common sections are well bounded, but flagged sections need repair/audit before trusting them as perfect generation input. Need prioritize fixing `foundEnd=false` / `foundStart=false` section OCR boundaries or add a generation guard that refuses to generate from flagged OCR without manual confirmation.

## Section OCR issue list
- Harrison asked to check and list which section OCR files have problems so he can inspect them.
- Ran a full meta audit over `workspace/materials/new-book-section-ocr/*.meta.json`.
- Summary: total 147 OCR files; 30 issue files. `START_NOT_FOUND`: 4 (`1`, `2`, `3`, `b`). `END_NOT_FOUND`: 25 (`1.1-2`, `1.4`, `1.4-3`, `1.7-1`, `1.7-5`, `1.7-8`, `1.10`, `1.12`, `2.5-1`, `2.6-4`, `2.7`, `3.1-1`, `3.3-5`, `3.4-1`, `3.5-1`, `3.7-1`, `3.8-3`, `3.9-3`, `3.11-4`, `3.13`, `b.1-2`, `b.5-2`, `b.5-6`, `b.7-7`, `b.8-10`). `MISSING_PAGES`: 0. `TOO_SHORT`: 1 (`b.7`).
- Spot-checked tails: `1.1-2` includes later Drill 1.1-1.3 content; `b.1-2` extends through complex logarithm/power formulas; `1.7-1` tail reaches impulse/step representation material; `b.7` is only the parent MATLAB heading and likely not a useful standalone lesson.

## Opened section OCR folder
- Harrison asked to open the folder containing the stored OCR files.
- Opened Finder at `/Users/chenghaoxiang/Desktop/tutor agent/workspace/materials/new-book-section-ocr`.

## B.7 OCR status clarification
- Harrison clarified: "b.7没问题".
- Treat `b.7` being very short as expected for the parent MATLAB entry/heading, not as an OCR issue. Future OCR issue lists should exclude `b.7` from the problem set unless the task specifically audits parent heading entries.

## Parent chapter OCR files clarification
- Harrison asked about `1`, `2`, `3`, and `b` START_NOT_FOUND entries, noting these should be chapter-level intro text and he could not find them in the folder.
- Confirmed the files exist as lowercase/simple filenames: `1.txt`, `2.txt`, `3.txt`, `b.txt` and matching `.meta.json` files under `workspace/materials/new-book-section-ocr`.
- Opened contents: `1.txt` contains Chapter 1 opening text before `1.1 SIZE OF A SIGNAL`; `2.txt` contains Chapter 2 opening text before `2.1 INTRODUCTION`; `3.txt` contains Chapter 3 opening text before `3.1 INTRODUCTION`; `b.txt` contains Background opening text before `B.1 COMPLEX NUMBERS`.
- These START_NOT_FOUND statuses are expected because the builder treats single-token chapter ids specially and starts at page beginning rather than requiring a normal numbered subsection heading. Do not count `1`, `2`, `3`, and `b` as broken small-section OCR.

## Parent OCR is overview/prelude only
- Harrison clarified with screenshot that parent entries like `b`/`1`/`2`/`3` are used to generate the section/chapter overview summary page, not regular lesson explanation content.
- Future audits for "生成讲解 OCR risk" should exclude parent overview/prelude entries and focus on actual subsection lesson files such as `1.1-2`, `b.1-2`, etc.

## Overview summary source clarification
- Harrison asked whether the current section overview summary is generated from the four parent OCR files `1`, `2`, `3`, and `b`.
- Checked current code. The visible handwritten overview summary in `getOverviewSummaryHtml()` uses `getSectionPreview(sectionId, sectionTitle)` and reads `preview.en`/`preview.zh` from `SECTION_PREVIEWS_NEW` embedded in `app/app.js` / sourced from `workspace/materials/new-book-previews.json`. It is not generated live from `1.txt`, `2.txt`, `3.txt`, or `b.txt`.
- Separate path: `loadChapterOverviewPrelude()` calls `/api/section mode='overview'`; the backend may read/generate a `parent_prelude` lesson cache using parent prelude OCR, and this only controls whether a parent lesson card/prelude lesson exists. This is distinct from the visible book-page summary text shown in the screenshot.

## OCR image/formula handling discussion
- Harrison said the parent overview/previews are fine and he will check other OCR files; then asked how OCR book images and formulas are handled.
- Need explain current architecture: formulas are OCR text first, normalized and repaired by `normalizeMathMarkdown` / prompt rules into LaTeX during Agent A/B generation; OCR formula fidelity can be imperfect and should be audited for key formulas. Images are handled separately from OCR: page images are served from new-book pages for Textbook view; extracted figure crops and metadata come from `workspace/materials/new-book-figures` plus `page-*.meta.json` / `section-figure-map-new.json`; Agent A sees available figure metadata and Agent B can render `book_image` references into markdown. OCR itself does not preserve images.

## Formula OCR quality check
- Harrison asked whether formula OCR is correct, how to check, and whether correctness can be guaranteed.
- Ran heuristic scan over `workspace/materials/new-book-section-ocr/*.txt` for formula-risk patterns: formula labels, integral signs rendered as `#`/`$`, lost superscript `2` patterns such as `|x(t)|2` and `x2(t)`, likely lost subscripts such as `C1`, and visible control/garbled characters.
- Cleaner scan found 100/147 files contain either formula labels or formula-risk patterns; top risk examples include `b.1-2`, `1.1-2`, `1.7-1`, `2.4-1`, `2.2`, `b.5-2`.
- Manually compared `workspace/materials/new-book-pages/page-065.png` against `new-book-section-ocr/1.1-1.txt` and `1.1-2.txt`. Actual formulas on page 065 include `E_x = ∫_{-∞}^{∞}|x(t)|^2 dt` and `P_x = lim_{T→∞} 1/T ∫_{-T/2}^{T/2}|x(t)|^2 dt`; OCR preserves labels and rough structure but renders integrals as `#`/`$`, loses superscript notation (`|x(t)|2`, `x2(t)`), and flattens limits/fractions (`limT→∞ T1 −T/2 ...`).
- Conclusion: cannot guarantee formula OCR is exactly correct. It is useful as a context signal, but standard lesson generation should not trust OCR formulas verbatim. Need formula audit/repair layer keyed by equation labels and page screenshots/PDF source before regenerating high-stakes formula-heavy lessons.

## Canonical formula table explanation
- Harrison asked what "canonical formula 表" means.
- Explain as an official per-section correct formula registry, separate from noisy OCR, containing equation id/label, exact LaTeX, source page, surrounding meaning, symbols, exam trigger, and optional notes. It lets Agent A/B use verified formulas instead of guessing from broken OCR.

## Desktop tutor agent takeover in tutor-agent-3 projectless thread
- Harrison asked in Chinese: "你看到我桌面上的tutor agent文件夹了吗，你读取记忆并接管".
- Confirmed the current generated Codex cwd `/Users/chenghaoxiang/Documents/Codex/2026-05-26/tutor-agent-3` is empty/non-canonical, and the active canonical project is `/Users/chenghaoxiang/Desktop/tutor agent`.
- Read project memory and takeover context from `workspace/memory/MEMORY.md`, today's `workspace/memory/2026-05-26.md`, recent `workspace/memory/2026-05-25.md`, root `README.md`, and `PROJECT_STRUCTURE.md`.
- Current understanding: runtime UI/API live in root `app/`, server entry is `app/ws-bridge.js`, lesson/cache/material work is mainly under `materials/` and `workspace/materials/`, and `workspace/memory/` is the persistent project memory that must be updated after each conversation.
- Current active state: `main` is ahead of `origin/main` by 5 commits; existing modified files include `app/app.js`, `app/index.html`, `app/style.css`, `app/ws-bridge.js`, `workspace/materials/lesson-cache/1_1-1/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`, and `workspace/memory/2026-05-21.md`; untracked include logo assets, lesson-cache folders `2_1`, `2_3`, `2_8`, and recent memory files. Preserve all existing changes and do not revert casually.
- Baseline verification in this takeover: `npm run check` passed, and `http://127.0.0.1:9000/health` returned ok with indexed pages `old=104`, `new=329`, `apiAsk=true`, and `apiTutor=true`.

## Home Ask redesigned from lesson-side Q&A reference
- Harrison provided screenshots of the Home Ask page and the lesson right-side Q&A panel, asking to call skills and redesign the main page Ask area by reference to the lesson Q&A.
- Used `frontend-design` and the Browser workflow. Added a final `TRUE EOF HOME ASK RIGHT-QA REDESIGN` CSS layer in `app/style.css`.
- Home Ask is now closer to the lesson-side Q&A structure: compact centered hero, small Formula/Screenshot/Exam Trap chips, bottom glass composer sized like a focused Q&A panel (`880x168` on 1728px desktop), compact attach/send controls, compact Balanced/web controls, and the old lower Concept Explanation / Study Materials / Problem Solving / Visualization button row is visually hidden.
- Moved the Home mode dropdown upward so it opens above the composer with a clean gap instead of covering the input area.
- Added a narrow-mobile protection breakpoint: below 720px the sidebar collapses visually and the main Home Ask content gets full viewport width, preventing the composer and title from being squeezed into a narrow column.
- Bumped `app/index.html` stylesheet cache to `style.css?v=1778674367`.
- Verification passed: `npm run check`, `git diff --check app/style.css app/index.html`, Playwright desktop QA at `1728x1050` confirmed no overlap between menu/composer/tags and hidden feature row, and mobile QA at `390x844` confirmed main content is no longer squeezed by the sidebar.

## Home Ask corrected to Harrison's exact compact screenshot layout
- Harrison said the previous Home Ask redesign was still too large and positioned wrong, and provided a target screenshot showing a compact mid-upper stack: small title/icons, compact chips, a smaller composer centered below, and the four tool buttons visible underneath.
- Used `frontend-design` again and added final `TRUE EOF HOME ASK SCREENSHOT MATCH` CSS in `app/style.css`.
- Corrected the layout: hero starts around the upper-middle of the content area, title uses smaller bold sans-serif, chips are compact, composer is `760x148` on a `1728x1050` desktop viewport, and the four feature buttons are restored in one row below the composer with natural widths so labels do not truncate.
- Adjusted the Home mode menu to open to the right and above the composer so it no longer overlaps the chips, composer, or feature buttons.
- Bumped stylesheet cache to `style.css?v=1778674368`.
- Verification passed: `npm run check`, `git diff --check app/style.css app/index.html`, `/health`, and Playwright layout QA. Final desktop metrics: hero `620x192` at y=158, composer `760x148` at y=395, feature row `820x43` at y=581, no component overlap, no text overflow, menu remains in viewport and avoids chips/composer/features.

## Home Ask fine tuning: smaller, frosted mode controls, lower position
- Harrison then asked to make the whole Home Ask main area slightly smaller, and later asked to make the three modes plus web-search control match the frosted-glass style shown in the lesson right-side Q&A screenshot.
- Added final `TRUE EOF HOME ASK GLOBAL SHRINK` CSS: title, chips, composer, mode controls, web button, and feature row were reduced together. Desktop QA after shrink measured hero y=142, composer `680x126` at y=346, feature row `700x37` at y=504.
- Added final `TRUE EOF HOME ASK MODE GLASS` CSS: `Fast / Balanced / Detailed` dropdown, selected mode row, current Balanced button, and web-search globe button now use translucent frosted glass gradients, blur, white borders, inner highlights, and soft shadows similar to the lesson Q&A panel.
- Harrison then asked to move the overall main page group down. Adjusted the final Home Ask stage top padding from about `13.5vh` to `18vh` and bumped cache to `style.css?v=1778674371`.
- Verification passed after these changes: `npm run check`, `git diff --check app/style.css app/index.html`, `/health`, and Playwright QA. Final desktop metrics after downshift: hero `620x163` at y=189, tags y=322, composer `680x126` at y=394, feature row `700x37` at y=552, with no overlaps.

## Desktop tutor agent takeover in tutor-agent-4 projectless thread
- Harrison asked in Chinese: "你看到我桌面上的tutor agent文件夹了吗，你读取记忆并接管".
- Confirmed the active canonical project is `/Users/chenghaoxiang/Desktop/tutor agent`; the generated cwd `/Users/chenghaoxiang/Documents/Codex/2026-05-26/tutor-agent-4` is just the projectless scratch workspace.
- Read project memory/context from `workspace/memory/MEMORY.md`, today's `workspace/memory/2026-05-26.md`, recent `workspace/memory/2026-05-25.md`, root `README.md`, `PROJECT_STRUCTURE.md`, and global `/Users/chenghaoxiang/Desktop/Memory/CODEX_ONBOARDING.md`.
- Current understanding: runtime UI/API live in root `app/`; server entry is `app/ws-bridge.js`; lesson generation has just been simplified so manual generation uses local deterministic section OCR only, with lesson-generation RAGFlow removed while `/api/ask` RAGFlow remains. Recent focus also includes Home Ask UI redesign, OCR section-boundary audits, and generated lesson cache/state preservation.
- Current repo state includes existing modifications to `app/app.js`, `app/index.html`, `app/style.css`, `app/ws-bridge.js`, `workspace/materials/lesson-cache/1_1-1/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`, and `workspace/memory/2026-05-21.md`, plus untracked logo assets, lesson-cache folders `2_1`, `2_3`, `2_8`, and recent memory files. Preserve these and do not revert casually.
- Baseline verification in this takeover: `npm run check` passed, and `http://127.0.0.1:9000/health` returned ok with indexed pages `old=104`, `new=329`, `apiAsk=true`, and `apiTutor=true`.

## Lesson generation discussion resumed
- Harrison said: "现在我们来讨论生成讲解的问题".
- Response should stay in discussion/architecture mode and not edit product code unless Harrison explicitly asks.
- Current framing to carry forward: lesson generation is now OCR-only for source context; the main problems to discuss are section OCR boundary reliability, Agent A/B prompt quality, generated lesson QA/coverage checks, visual/example density, cache/version strategy, and whether generation should refuse flagged OCR sections.

## Q&A Windows RAG direction
- Harrison said: "第一步，我要我的问答接入windows的rag逻辑，你觉得没问题吧".
- Architectural judgment: yes, this is the right split. Lesson generation should stay deterministic OCR-only because the section is known and needs full coverage, while user Q&A should use Windows RAGFlow semantic retrieval because the query is open-ended and benefits from cross-section retrieval/citations.
- Important nuance: keep the two pipelines isolated so enabling Windows RAG for `/api/ask` does not re-enable lesson-generation RAG. Current recent work already removed lesson-generation RAG hooks while preserving general RAGFlow helpers and `/api/ask` behavior.

## Q&A Windows RAG stabilized
- Harrison asked to directly fix Q&A Windows RAG to stable usable state and asked whether lesson-side Q&A should use current section OCR or direct RAG.
- Architecture decision implemented: lesson-side Q&A uses current section OCR as the hard anchor and Windows RAGFlow as supplemental semantic retrieval. This avoids losing the current small section while still supporting cross-section questions.
- Edited `app/ws-bridge.js`: added `mergeAskBookContexts()`, changed `/api/ask` so section Q&A prioritizes per-section OCR via `attachSectionOcrToPages()` when `sectionId/sectionTitle` is present, and changed RAGFlow success behavior from replacing `relatedBooks` to appending RAGFlow chunks after local OCR. Debug now reports `localBookContextCount`, `ragFlowBookContextCount`, and `finalBookContextCount`.
- Verified Windows side after Harrison said it was ready: Mac userspace Tailscale sees Windows `msi` (`100.106.128.64`) active, `curl --proxy http://127.0.0.1:1056 http://100.106.128.64:9380/` returns RAGFlow's expected 404 JSON, and retrieval API returns `code=0` with 5 chunks for "What is signal energy?".
- Verified `/api/ask` on temporary port 9108 with `sectionId=1.1-1`, `bookSource=new`, `useWebSearch=false`: response debug showed `ragFlowEnabled=true`, `ragFlowUsed=true`, `ragFlowChunkCount=5`, `localBookContextCount=1`, `ragFlowBookContextCount=5`, `finalBookContextCount=6`; answer generated successfully after OpenRouter became available again.
- Verification: `npm run check` passed and `git diff --check app/ws-bridge.js` passed.
- Operational note: userspace Tailscale proxy is now running in detached screen session `fourier-tailscale`, exposing SOCKS `127.0.0.1:1055` and HTTP proxy `127.0.0.1:1056`. `launchctl` was attempted but blocked by macOS Desktop path permissions/provenance; screen worked.

## Web search implementation discussion
- Harrison asked: "那我联网搜索呢，这个要怎么实现".
- Checked current code: `/api/ask` already has a Web Search pipeline controlled by `useWebSearch`; it calls `generateSearchAngles()`, then `collectWebSources()`, then injects `buildWebContext(webSources)` into `generateExplanation()`. Frontend already passes `useWebSearch` from the Web toggle and renders `liveSearchEvents` / `webSources`.
- Current limitation: `collectWebSources()` mostly depends on Serper (`SERPER_API_KEY`) plus Wikipedia; DuckDuckGo helper exists but is not meaningfully used by `buildSearchPlan()` because all non-Wikipedia plan entries are Serper. If `SERPER_API_KEY` is absent, web search quality collapses to mostly Wikipedia/no sources.
- Recommended architecture: keep web search as optional third layer after current section OCR and Windows RAGFlow. OCR = section anchor, RAGFlow = textbook semantic retrieval, Web = external/current/supplementary sources. Do not let web replace textbook context.

## Web search stabilized with Serper
- Harrison said "做" to implement web-search stabilization, then provided a Serper API key and asked if anything else was needed.
- Added `SERPER_API_KEY` to local ignored `app/.env` (do not print or commit key).
- Edited `app/ws-bridge.js` Web Search chain only:
  - `buildSearchPlan()` now includes DuckDuckGo fallback entries alongside Serper for video/visual/course/insight/general queries and de-duplicates provider/query pairs.
  - Added `shouldKeepWebSource()` to filter search pages and low-quality/social/answer-farm/community sources like Google/Bing search pages, Pinterest/Facebook/TikTok/X, Reddit/Quora, CourseHero/Chegg/Brainly/Quizlet.
  - `collectWebSources()` now tracks `providerStats`, `providerErrors`, `providersUsed`, `serperConfigured`, and returns `{ sources, debug }` instead of a bare array.
  - `/api/ask` now preserves the frontend `webSources` array behavior while adding debug fields: `webSearchEnabled`, `webSearchUsed`, `webSourceCount`, `webProvidersUsed`, `webProviderStats`, `webProviderErrors`, and `serperConfigured`.
- Verification:
  - Direct Serper curl returned 5 organic results for "signal energy visual explanation lecture notes"; first result was "Energy Signals & Power Signals - YouTube".
  - Temporary `/api/ask` on port 9110 with `useWebSearch=true`, `sectionId=1.1-1`, `bookSource=new` succeeded with OCR + RAGFlow + Web: `ragFlowChunkCount=5`, `localBookContextCount=1`, `ragFlowBookContextCount=5`, `finalBookContextCount=6`, `webSearchUsed=true`, `webSourceCount=11`, `webProvidersUsed=["serper"]`, `serperConfigured=true`.
  - Returned web sources included YouTube, 3Blue1Brown, BetterExplained, Medium visual article, and university lecture-note PDF sources.
  - `npm run check` and `git diff --check app/ws-bridge.js` passed.
- Note: the test question explicitly asked for a visual explanation, so gptimage2 image generation was triggered and timed out after the answer was generated. This is separate from Web Search and does not block the text answer.

## Main-page Q&A path confirmed
- Harrison asked: "那主页面的问答呢".
- Confirmed frontend main-page `sendQuestion()` also calls the same `/api/ask` endpoint and passes `useWebSearch` from `webSearchToggleBtnMain`; main-page first questions have no `sectionId/sectionTitle`, so they do not use a current-section OCR anchor. Instead, backend uses local OCR keyword retrieval, then appends Windows RAGFlow semantic chunks, then optional Serper Web Search.
- Tested main-page-style `/api/ask` request on temporary port 9111 with no section fields: "Where does the textbook define convolution and can you explain it intuitively?", `bookSource=new`, `useWebSearch=true`.
- Result: success with `localBookContextCount=5`, `ragFlowChunkCount=5`, `ragFlowBookContextCount=5`, `finalBookContextCount=10`, `webSearchUsed=true`, `webSourceCount=9`, `webProvidersUsed=["serper"]`, `serperConfigured=true`. Web sources included YouTube, MIT OCW, Signals and Systems PDF, and convolution teaching references.
- Small polish: changed response step text so no-section main-page Q&A says `相关教材 OCR` instead of `当前小节 OCR`.
- Verification after polish: `npm run check` and `git diff --check app/ws-bridge.js` passed.

## Main-page OCR vs bare RAGFlow architecture question
- Harrison asked what "相关教材 OCR" means and then asked whether main-page Q&A should just use bare RAGFlow instead of requiring local OCR.
- Current judgment to carry forward: for main-page general Q&A, RAGFlow should be primary because the question is open-ended and semantic retrieval is the right tool. Local OCR keyword/page retrieval should be demoted to fallback or lightweight supplement, not treated as an equal first-class context source.
- Recommended split: lesson-side Q&A keeps current-section OCR as anchor + RAGFlow supplement; main-page Q&A uses RAGFlow-first, with local OCR only if RAGFlow fails or returns too few chunks. This avoids duplicated/noisy context and lets the Windows RAG layer do the thing it was built for.

## Main-page Q&A changed to RAGFlow-first
- Harrison agreed to make main-page Q&A RAGFlow-first and asked whether >90% of cases will directly use RAGFlow.
- Implemented in `app/ws-bridge.js`: `/api/ask` now splits by context:
  - `sectionId/sectionTitle` present: keep section OCR anchor, then append RAGFlow chunks.
  - no section and no attachment-first: main-page Q&A tries RAGFlow first. If RAGFlow returns chunks, those chunks become the textbook context directly with `localBookContextCount=0`. If RAGFlow fails/returns empty, fall back to local OCR keyword/page retrieval.
  - attachment-first: keep attachment-driven local OCR selection.
- Added debug fields `textbookContextStrategy` and `usedLocalFallback`.
- Increased local ignored `app/.env` `RAGFLOW_TIMEOUT_MS` from 8000 to 20000 because the first RAGFlow-first test fell back after an 8s Windows/Tailscale timeout; after increasing timeout, main-page test succeeded as pure RAGFlow-first.
- Verification after timeout increase: temporary `/api/ask` on port 9113 with no section fields for convolution returned `textbookContextStrategy="ragflow_first"`, `ragFlowUsed=true`, `ragFlowChunkCount=5`, `localBookContextCount=0`, `ragFlowBookContextCount=5`, `finalBookContextCount=5`, `usedLocalFallback=false`, `webSourceCount=10`.
- Polished step text so pure RAGFlow-first says `RAGFlow 检索到 N 个教材片段` instead of `相关教材 OCR 0 页 + RAGFlow...`.
- Verification: `npm run check` and `git diff --check app/ws-bridge.js` passed.

## Student personalization database concern
- Harrison raised a product/architecture concern: Signal/Fourier Agent may need a backend database once serving 5-10 students, because real teaching issues become case studies that reveal student sticking points, background knowledge, thinking habits, and personalization needs.
- Response should frame this as a real product inflection point: the system needs to store not only profiles, but learning telemetry, misconceptions, interventions, outcomes, and cohort-level teaching insights.
- Recommended direction to carry forward: keep standard lesson cache profile-free, but introduce a real DB-backed student memory and case-study layer. Store raw conversations minimally/with consent, distill each session into structured learning events, maintain per-student memory plus anonymized aggregate insight for improving lessons/prompts.

## Next step for personalization DB
- Harrison asked: "所以你下一步要干嘛".
- Recommended next move: build the minimal durable personalization foundation, not a complex UI first. Specifically: audit existing `userMemory`/quiz/session storage, design a small local DB schema for students/sessions/messages/learning_events/student_memory/concept_stats, implement append-only event logging after `/api/ask`, then add an LLM distiller that converts each Q&A turn into structured learning events and updates student memory.
- Key principle: standard generated lessons stay profile-free; personalization lives in Q&A, student memory, and aggregate case-study insights.

## Learning Preference reset/merge behavior implemented
- Harrison asked what happens if he already wrote a Learning Profile and then resets/reruns Quick Setup.
- Implemented in `app/app.js`:
  - Manual saves now mark `preferenceProfile.source = "manual"` and `manualEdited = true`.
  - Quick Setup completion now builds a quiz-derived Learning Profile when no manual profile exists.
  - If a manual profile exists, Quick Setup does not overwrite it. It appends/replaces a `Latest Quick Setup` block below the manual profile and includes a `Priority Rule` saying the manually written profile has higher priority.
  - Default profiles now carry `source = "default"` and `manualEdited = false`.
  - `resetQuiz()` now sends `resetQuiz: true`, clears local `userMemory.quiz`, removes `localStorage.tutorQuiz`, and opens Quick Setup without deleting the saved Learning Profile.
- Implemented in `app/ws-bridge.js`: `/api/memory` now treats `resetQuiz: true` as a real reset by setting `existing.quiz = {}` and `quizResetAt`, instead of normalizing `{}` into a complete default quiz.
- Verification: `npm run check` passed; `git diff --check -- app/app.js app/ws-bridge.js` passed.

## Learning Profile reset navigation fix
- Harrison reported that after resetting Learning Profile from settings, finishing Quick Setup returned to Home instead of the settings page.
- Cause: `resetQuiz()` opened the global quiz overlay, and the global quiz completion handler always called `showWelcome()`.
- Fixed in `app/app.js`: added `quizReturnView`, made `showQuiz({ returnView })` remember the caller, changed `resetQuiz()` to `showQuiz({ returnView: "settings" })`, and changed quiz completion to return to Settings (or Preference if used later) before falling back to Home.
- Verification: `npm run check` passed.

## Answer workspace liquid glass UI pass
- Harrison asked to turn the screenshot interface into a frosted/glassmorphism style and explicitly asked to use the frontend-design skill.
- Used `frontend-design` skill. Implemented in `app/style.css` as an EOF override block:
  - Added answer glass variables and liquid-glass surfaces.
  - Restyled the app sidebar, logo pill, nav links, topbar, answer steps, user/assistant answer bubbles, references, loading/search progress card, and follow-up composer.
  - Converted the old paper/notebook loading card into a translucent glass panel with soft highlights, glass borders, blur, and lighter progress rows.
- Verification: `npm run check` and `git diff --check -- app/style.css app/app.js app/ws-bridge.js` passed.
- Previewed with Playwright by forcing the answer screen/loading state and saved a local preview at `/Users/chenghaoxiang/Documents/Codex/2026-05-26/tutor-agent-4/answer-glass-preview-2.png`.

## Learn right Q&A composer glass pass
- Harrison asked to also change the lesson right-side Q&A input bar into a frosted/glass style, again using the frontend-design skill.
- Implemented in `app/style.css`:
  - Added a final `LEARN RIGHT Q&A COMPOSER LIQUID GLASS LOCK` for `#learnFollowupBar`.
  - Added a duplicated-ID high-specificity override because `app.js` injects runtime CSS with duplicated IDs that previously beat normal stylesheet rules.
  - The right-side composer now uses 28px radius, translucent glass gradients, blur/saturate backdrop, inner highlights, glass icon buttons, and glass mode/web controls.
- Verification: `npm run check` and `git diff --check -- app/style.css app/app.js app/ws-bridge.js` passed.
- Playwright computed-style check confirmed `#learnFollowupBar` has `border-radius: 28px`, `backdrop-filter: blur(36px) saturate(1.95) brightness(1.06)`, and the new glass background/box-shadow.

## Key Takeaways and Quick Check high-transparency glass pass
- Harrison asked to redo the lesson `Key Takeaways` and `Quick Check` blocks with frontend-design skill and make transparency higher.
- Implemented in `app/style.css` as a final `LESSON CARDS HIGH-TRANSPARENCY GLASS PASS`:
  - Reduced white opacity on `.lecture-note-card-summary`, `.learn-key-takeaways-list > li`, `.lesson-test-banner`, and `#startTestBtn`.
  - Increased blur to 42-44px on large cards, 34px on takeaway items, and 30px on the quick-check button.
  - Kept borders/highlights readable while making the cards feel more transparent over the notebook/page background.
- Verification: `npm run check` and `git diff --check -- app/style.css app/app.js app/ws-bridge.js` passed.
- Playwright computed-style check confirmed low-alpha backgrounds: summary `rgba(255,255,255,0.26/0.075/0.04)`, takeaway item `0.204/0.055`, quick check `0.243/0.075/0.035`.

## Canonical formula catalog pilot for lesson generation
- Harrison approved the plan to add a per-section canonical formula table so lesson generation can keep OCR as the prose/source context while using visually verified LaTeX for formulas.
- Implemented a minimal pilot for new-book sections `1.1-1` and `1.1-2`.
- Added `workspace/materials/formula-catalog/README.md`, `1.1-1.formulas.json`, and `1.1-2.formulas.json`.
- Manually visually checked formulas against page images `page-065` through `page-070`. `1.1-1` now has 2 verified formulas; `1.1-2` has 11 verified formulas injected into generation plus one non-injected `derived_from_text` periodic-power rule kept as a note.
- Edited `app/ws-bridge.js`: added `FORMULA_CATALOG_DIR`, `readVerifiedFormulaCatalog()`, and `buildVerifiedFormulaPromptSection()`. Agent A now receives a `verified_canonical_formulas` section before OCR pages, and Agent B receives the same verified formula payload in JSON.
- Added a lesson rule that verified canonical formulas override lossy OCR rendering, so Agent A/B should copy exact LaTeX and labels instead of reconstructing formulas from OCR.
- Scope guard: formula catalog loading is new-book only and only injects entries with `"status": "verified"`.
- Verification passed: `npm run check`, `git diff --check app/ws-bridge.js workspace/materials/formula-catalog/...`, and JSON parsing/count checks.

## Learn right Q&A message/error glass pass
- Harrison noticed the lesson right-side Q&A response/error bubble still looked like an old solid card even after the input composer was changed.
- Root cause: the earlier pass restyled `#learnFollowupBar` only; generated Q&A messages use `.followup-bubble`, `.fub-q`, `.fub-a`, and `.error-box`, with additional high-specificity active-chat rules elsewhere in `style.css`.
- Implemented an EOF `LEARN RIGHT CHAT MESSAGE GLASS LOCK` in `app/style.css`:
  - User question bubble `.fub-q` now has translucent blue glass, blur, and glass border.
  - Assistant answer container `.fub-a` now has high-transparency glass background, blur, inner highlight, and proper padding even in active chat mode.
  - Error card `.error-box` now uses the same frosted glass treatment, so failures like model-region HTTP 403 no longer render as a flat old card.
- Verification: `npm run check` and `git diff --check -- app/style.css app/app.js app/ws-bridge.js` passed.
- Playwright computed-style check on a forced right-chat error state confirmed `.fub-q`, `.fub-a`, and `.error-box` all have `backdrop-filter: blur(...)` and glass borders/background layers.

## Textbook focus overlay glass pass
- Harrison asked to make the full-screen textbook preview Q&A panel, Q&A floating button, and page counter glassmorphism too.
- Implemented in `app/style.css` as a final `TEXTBOOK FOCUS GLASS LOCK` scoped to `#textbookFocusModal`.
- Restyled:
  - `#textbookFocusQaPanel` with high-transparency glass, 42px blur, inner highlight, and softer border.
  - `#textbookFocusQaToggle` with circular blue glass instead of a solid blue button.
  - `#textbookFocusPageIndicator` with translucent glass instead of the old paper tag.
  - Textbook-focus Q&A header, close button, empty state, compose bar, input, web/mode controls, send button, and copied chat bubbles.
- Verification: `npm run check` and `git diff --check -- app/style.css app/app.js app/ws-bridge.js` passed.
- Playwright computed-style check on a forced textbook-focus state confirmed `#textbookFocusQaPanel`, `#textbookFocusQaToggle`, `#textbookFocusPageIndicator`, `.textbook-focus-qa-compose`, `.fub-q`, and `.fub-a` all have glass background layers and `backdrop-filter: blur(...)`.

## v1.2.0 production deployment
- Harrison asked to deploy the current version to the server and bump the visible version to `v1.2.0`; then clarified the project name should be `Fourier`.
- Updated package versions in root `package.json`, `package-lock.json`, and `workspace/package.json` to `1.2.0`.
- Updated visible app version in `app/index.html` sidebar and Settings to `v1.2.0`; bumped frontend asset query params to `style.css?v=1200` and `app.js?v=1200`.
- Added the referenced `app/logo.png` to Git because the current HTML uses `logo.png`.
- Pushed release commits to GitHub main:
  - `ce462b8 release: v1.2.0`
  - `02a2999 chore: rename product to Fourier`
- Vercel production initially failed because the project root directory was still configured as the old `tutor-openclaw-ui` folder. Used Vercel CLI/API to link the local repo to `mrparameciums-projects/aquarius`, clear the Vercel project `rootDirectory` to repo root, then ran `vercel pull --environment production`, `vercel build --prod`, and `vercel deploy --prebuilt --prod`.
- Production deployment succeeded: `dpl_J8siA4nm5jRxX3JnGSi2fgbN4iDn`, aliased to `https://aquarius-seven.vercel.app`.
- Verified the production HTML directly: title is `Fourier · Signal Processing & Linear Systems`, sidebar shows `v1.2.0`, Settings shows `Fourier Tutor Agent v1.2.0`, and assets load with `style.css?v=1200` / `app.js?v=1200`.
- Note: `/health` on the Vercel alias returns `NOT_FOUND` because this Vercel project serves the static frontend; backend health belongs to the separate bridge/server deployment path.
