{"file": ".trellis/spec/css/verification.md", "reason": "How to add a per-view css-probe state (assert-as-entered AND assert the cascade winner / R8), probe every touched property, the two mandatory gates, baseline-before-edit, noise-floor caveat, set-difference/report-diff techniques"}
{"file": ".trellis/spec/css/cascade-and-collapse.md", "reason": "NOCOMP/LOSES/DEFENSIVE classification (competitor-backed = DEFENSIVE = keep) + scope-by-token (Rule 1). NOTE: the empirical keep-set/strip-grow convergence loop is NOT documented in any spec — for D2's floor proof follow this task's design.md §3 + implement.md Phase 2 + the _strip-view-important.js / _grow-keep-from-report.js / _view-cascade-probe.js tool headers"}
{"file": "tools/css-probe.js", "reason": "D1 target: PROBE_STATES conventions (append the feedback state LAST — it is the first cross-view nav), runner enters guest mode once then loops, fails-closed on __MISSING__/__ABSENT__, no per-state fixture restore, and the R8 winner-sentinel contract (header L33-35 + assertFollowupBarWinner=152px / S12 glass token). Fixture via page.route('**/api/feedback') interception — the board is server-fetched (loadFeedbackBoard), not localStorage"}
{"file": "tools/_view-cascade-probe.js", "reason": "D2 arbiter (feedback already VIEWS[0], seeded fixture + 9 interactions: rest+5 hover+3 focus); the byte-identical strip/proof gate; coverage-matrix blind spots (state outside themes×5 viewports×9 interactions invisible — its own L99-102 comment force-keeps submit :active/:disabled it cannot probe) + inline-style caveats in its header. When resetting the feedback keep-set, PRESERVE the 15 arbiter-unreachable carve-out decls (cross-view L24702 + state-gated :active/:disabled/.is-target/un-hovered) — reset only the 457 reachable lines; fix the stale courseTracker/settings docstring while open"}
{"file": ".trellis/spec/app/conventions.md", "reason": "Repo rules: vanilla CSS/JS, no build step, npm run check, Windows-illegal filenames. (npm run check's package.json script node --checks 53 files INCLUDING tools/css-probe.js, so it covers the D1 edit — CLAUDE.md's 'only ws-bridge.js + app.js' line is stale. scope-by-token lives in css/cascade-and-collapse.md Rule 1, not here.)"}
